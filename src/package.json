{
  "name": "omni-extension-pdf2doc",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "build": "esbuild ./extension.js --bundle --outdir=../server --format=esm --platform=node --target=es2020 --external:omni-sockets --analyze --color=true"
  },
  "dependencies": {
    "pdf2json": "^3.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}
